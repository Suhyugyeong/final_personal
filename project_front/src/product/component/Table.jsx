import React from "react";

const Table = ({ productData }) => {

  return (
    // 테이블 렌더링을 위한 JSX 코드
    {product.data.map((product)=>
        <tr></tr>())}
      <tr key={product.auctuin_id}>
  );
};

export default Table;
